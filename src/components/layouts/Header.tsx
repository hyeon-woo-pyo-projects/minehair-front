import { BrowserRouter, Link } from 'react-router-dom';

function Header () {
    return (
        <BrowserRouter>
            <header></header>
        </BrowserRouter>
    )
}

export default Header;